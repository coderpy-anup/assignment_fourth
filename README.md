# Files, Exceptions, and Errors in Python

## 📄 Task Description

This Python script reads and displays the contents of a text file named `sample.txt`. It also gracefully handles scenarios where the file does not exist.

---

## ✅ Features

- Reads a text file line by line
- Prints each line to the console
- Handles `FileNotFoundError` and other exceptions gracefully

---

## 🚀 How to Use

1. Make sure you have Python installed (Python 3.x recommended).
2. Place your `sample.txt` file in the same directory as the script.
3. Run the script using the terminal or command prompt:

```bash
python read_file.py
```


# 📄 Task 2: Write and Append Data to a File

## 🧠 Problem Statement

Write a Python program that:

1. Takes user input and writes it to a file named `output.txt`.
2. Appends additional data to the same file.
3. Reads and displays the final content of the file.

---

## ✅ Features

- Accepts user input and saves it to a file.
- Appends more user input to the same file.
- Reads and displays the entire content of the file after writing and appending.

---

## 📂 Files Included

- `task_2.py`: The main Python script.
- `output.txt`: Output file generated during script execution.

---

## 🚀 How to Run

1. Ensure you have Python 3.x installed.
2. Open your terminal or command prompt.
3. Run the script using:

```bash
python task_2.py
```